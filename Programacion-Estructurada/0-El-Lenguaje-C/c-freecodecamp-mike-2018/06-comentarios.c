#include<stdio.h>
#include<stdlib.h>

int main(){


	/*
	 * Commentarios de multiples lineas
	 * Vim automaticamente pone simbolos "*" pero solo es para
	 * que los comentarios de multiples lineas sean mas agradables
	 * a la vista.
	 */

	/*
	Como veras, siempre se pueden borrar,
	no hay ningun problema
	 */

	//comentarios de una linea
	//vim tambien automatiza la escritura de "//", pero esta vez para fines practicos
	//automaticamente convierten la nueva linea en un comentario de una linea
	
	printf("Comments are fun");

	return 0;
}
