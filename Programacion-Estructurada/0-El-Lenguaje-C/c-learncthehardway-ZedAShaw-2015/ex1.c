#include <stdio.h> //esta es una forma de importar contenidos de un archivo a este archivo. C tiene la convención de usar extensiones .h para archivos "header", los cuales contienen listas de funciones para usar en tu programa.


/* Esto es un comentario multilineas. */

int main(int argc, char *argv[])
{
	int distance = 100;

	// Esto es un comentario de una linea
	printf("Usted esta a %d kilometros de distancia.\n", distance);

	return 0;
}
